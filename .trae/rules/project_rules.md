1. 项目采用 Python 3.11 版本。
2. 项目采用 UV 作为依赖管理工具。
3. 项目采用 Pydantic 作为数据验证工具。
4. 项目采用 Ruff 作为代码风格检查和格式化工具。
5. 每次实现了新功能后，都要写对应的测试用例，新的测试用例也保存起来，且运行一遍所有测试，确保新功能没有引入错误。
6. 注意模块化设计，每个模块负责特定的功能，避免模块之间的耦合度过高，单个代码文件尽量不要超过两千行。
7. 条件语句（if、else、elif）要保持简洁，避免嵌套过深，不要超过三层。